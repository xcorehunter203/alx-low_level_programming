#include <stdio.h>
/**
  *main - main is the entry point
  *Return: return 0 (succsess)
  */
int main(void)
{
	char ch[] = "_putchar";

	for (int i = 0; i <= 8 ; i++)
		putchar(ch[i]);
	putchar('\n');
	return (0);
}
