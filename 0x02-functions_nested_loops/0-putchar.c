#include "main.h"
/**
  *main - main is the entry point
  *Return: return 0 (succsess)
  */
int main(void)
{
	char ch[] = "_putchar";

	for (int i = 0; i < 8 ; i++)
		_putchar(ch[i]);
	_putchar('\n');
	return (0);
}
